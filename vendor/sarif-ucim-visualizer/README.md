# Sarif UCIM visualizer (CRA)

Source for the interactive UCIM embed on `/about`. Production assets are copied to `public/ucim-visualizer` by `npm run sync:ucim` (run automatically from repo root `npm run build`).

To work on the app locally: `cd frontend && npm install --legacy-peer-deps && npm start`.
