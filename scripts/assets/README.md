# Build-time assets (not served from /public)

- `og-source-branded-master.png` — master raster for `scripts/generate-og-image.mjs`

These files are intentionally kept out of `public/` so they are never downloaded by site visitors or social crawlers.
