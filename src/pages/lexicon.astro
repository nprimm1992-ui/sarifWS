---
/**
 * /lexicon/ — Sarif's operating vocabulary as a published artifact.
 *
 * The Atlas is the sole surface: the graph, its controls, and the
 * term inspector. Filter state is persisted to the URL (`?q=…&cat=…`)
 * by the page island so links share state and back/forward restore it.
 * Hash arrivals (`/lexicon/#coherence-decay`) and `?term=` deep links
 * select and open the matching node in the inspector.
 */
import Base from '../layouts/Base.astro';
import LexiconAtlas from '../components/lexicon/LexiconAtlas.astro';
import { buildLexiconGraph } from '../lib/lexicon-graph';

const graph = await buildLexiconGraph();
---

<Base
  title={`Lexicon ${graph.version} — Sarif Consulting`}
  description={`Sarif Consulting's operating vocabulary. ${graph.count} terms, ${graph.categoryCount} categories, version ${graph.version}. Filterable, machine-readable.`}
  noindex
>
  <article class="lex-page" data-lex-page>

    <!-- ══════════════════════════  ATLAS  ═════════════════════════════ -->
    <!-- Primary and sole surface: the graph, its controls, metadata
         readout, and the term inspector — all in one backdrop. -->
    <LexiconAtlas />

    <!-- Live region for ARIA announcements from the island. -->
    <div class="visually-hidden" role="status" aria-live="polite" data-lex-announce></div>
  </article>
</Base>

<script>
  import '../scripts/lexicon-page';
</script>

<style>
  .lex-page {
    display: block;
    --lex-nav-h: 72px;
  }
</style>
